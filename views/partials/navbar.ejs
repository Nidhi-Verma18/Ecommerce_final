<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand fs-3 fw-bolder"  href="#">Shopping Cart</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link active" aria-current="page" href="/products">Home</a>
        <% if(currentUser && currentUser.role=='seller'){ %>
          <a class="nav-link active" aria-current="page" href="/product/new">New</a>
       <% } %>
        
      </div>

      <!-- Add ms-auto to push the below section to the end of the navbar -->
      <div class="d-flex ms-auto" role="search">
        <% if(!currentUser){ %>
          <a href="/login" class="nav-link">Login</a>
        <% } else{ %>
          <a href="#" class="text-dark">Hii,<%=currentUser.username %></a>
          <a href="#" class="nav-link mx-2">
            <i href='/user/cart' class="fa-solid fa-cart-shopping   fs-4 text-dark"></i>
            <sup><span class="badge bg-danger"><%= currentUser.cart.length %></span></sup>
          </a>
          
          <a href="/logout" class="nav-link text-dark">Logout</a>
          
          
        <% } %>
       
      </div>
    </div>
  </div>
</nav>
